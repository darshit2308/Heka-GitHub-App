# The App ID you just copied
APP_ID=4115704

# The Webhook Secret you created earlier
WEBHOOK_SECRET=heka_dev_secret_2026

# Open the downloaded .pem file in a text editor, copy everything 
# (including the BEGIN and END lines), and paste it here.
# Note: For Probot, it's best to wrap the whole key in quotes and use \n for newlines,
# OR just point to the file path if your prototype was set up that way:
PRIVATE_KEY_PATH=./private-key.pem